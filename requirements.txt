# Agent / LangGraph runtime
ffmpeg-python==0.2.0
grequests==0.7.0
tabulate==0.9.0
python-dotenv==1.0.1
rich==13.7.0
aiosqlite==0.20.0
langchain-openai>=0.1.7
langchain>=0.2.1
langgraph>=0.0.65
langgraph-cli>=0.1.35
langgraph-sdk>=0.1.11
openai>=1.30.3

# Open-source video indexing + retrieval
qdrant-client>=1.9.0
scenedetect[opencv]>=0.6.4
decord>=0.6.0
numpy>=1.24.0
Pillow>=10.0.0

# Local visual encoder (CLIP) + grounding model dependencies
transformers>=4.40.0
torch>=2.0.0
accelerate>=0.28.0

# 4-bit quantization for the TRACE grounding model on T4-class GPUs
bitsandbytes>=0.43.0

# NOTE: The TRACE grounding backend additionally requires installing
# https://github.com/gyxxyg/TRACE.git as an editable package, because TRACE
# ships a custom `TraceMistralForCausalLM` architecture not in stock
# transformers. Run, after `pip install -r requirements.txt`:
#
#     git clone https://github.com/gyxxyg/TRACE.git
#     cd TRACE && pip install -r requirements.txt && pip install -e .
#
# To use the fine-tuned QDDETRHead grounding backend instead, no extra
# install is needed (the head is defined in jockey/open_source/training/).
