VITE_API_BASE_URL=http://localhost:8085/api/v1
# Standalone /s3-test page backend (scripts/s3_browser.py); leave unset to use the default.
# VITE_S3_BROWSER_URL=http://localhost:8765
# Google OAuth Web Client ID (from Google Cloud Console). Shows the "Continue
# with Google" button. Must match GOOGLE_CLIENT_ID on the iam backend. This is a
# public value (sent to the browser), so it's safe to commit.
VITE_GOOGLE_CLIENT_ID=1085042568418-ofevgqn4rhhomju9o9erbfr87eu5fek6.apps.googleusercontent.com
